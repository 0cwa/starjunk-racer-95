# Performance baselines

No baseline is checked in yet.

The first authoritative baseline must be captured on a named repeatable GPU runner with a documented OS/GPU/driver/browser or native rendering driver. Commit it only after reviewing the scene visually and confirming the run is uncapped.
