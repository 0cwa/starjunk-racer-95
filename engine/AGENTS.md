# Engine subtree instructions

This directory is for engine source pins, patch series and forward-port documentation only.

Never put gameplay features here. Prefer changes that can be upstreamed or deleted when upstream Godot gains equivalent WebGPU support. Every engine change must name the pinned upstream base and include a focused validation step.
