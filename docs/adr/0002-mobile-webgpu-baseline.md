# ADR 0002: Mobile renderer as shared visual baseline

Status: accepted

Design the core visual language for Godot's Mobile RenderingDevice renderer. Forward-port a WebGPU backend so the browser can use that renderer. Native Forward+ may add optional effects, but core readability/aesthetic identity must survive on Mobile/WebGPU.
